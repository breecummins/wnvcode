function V = V2Parametric_squigglyR(x,y,Vmag,t)
	%Parametric definition of the wind along the x-axis
  
	a = 0.05*pi;
	b = sqrt(3)/(2*a);
	c = 1/2;
	
	corterm=1;
	
	V = (b*Vmag*sin(a*y)/110 + c*Vmag)*corterm;
