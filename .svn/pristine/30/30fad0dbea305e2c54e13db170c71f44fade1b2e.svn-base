%makepictures_2groups.m

basedirold = '~/WNV_Archived/WNVNewHostSeeking/ChangingNumHostsAverageChickPos';
basedir = '~/WNVNewHostSeeking_CorrectedInterpolation/ChangingNumHostsAverageChickPos';

% %consolidate data and calculate statistics (only needs to be done once)
% collateSimdata_2groups(basedir);

%load the file made in collateSimdata_density and make pictures. Return values are the y-intercepts (P0 in paper).
plot2groupratios(basedir);